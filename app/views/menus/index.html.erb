<p style="color: green"><%= notice %></p>

<h1>List of Menu</h1>

<div id="menus">
  <% @menus.each do |menu| %>
    <%= render menu %>
        
    <p>
      <%= link_to "Show this menu", menu %>
    </p>
  <% end %>
</div>

<%= link_to "New menu", new_menu_path %>
